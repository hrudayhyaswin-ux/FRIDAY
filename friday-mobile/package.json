{
  "name": "friday-mobile",
  "version": "1.0.0",
  "description": "FRIDAY AI — Personal Offline Mobile Assistant",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static site - no build needed'",
    "add:android": "npx cap add android",
    "add:ios": "npx cap add ios",
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "run:android": "npx cap run android",
    "run:ios": "npx cap run ios"
  },
  "keywords": ["friday", "ai", "offline", "assistant", "mobile"],
  "author": "hrudayhyaswin-ux",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
